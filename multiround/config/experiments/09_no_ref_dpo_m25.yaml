# 09. DPO without Reference Update - Margin 25, Full 5 Rounds
# Tests full 5-round training without reference model updates

inherit_from: multiround/config/multiround_defaults.yaml

seed: 42
device: cuda
loss_type: dpo

wandb:
  enable: true
  project: MultiRound-DPO-RNA
  entity: <wandb-entity>
  run_name: 09_no_ref_dpo_m25_5r
  mode: online
  tags: ["09_no_ref", "margin25", "5_rounds", "ablation"]
  notes: "DPO margin25 without reference updates - full 5 rounds"
  group: "ablation_experiments"

multiround:
  num_rounds: 5
  epochs_per_round: 20
  update_reference: false      # Key difference: no reference updates
  eval_temperature: 0.5
  final_eval_temperature: 0.1
  n_samples_eval: 8
  n_samples_final_eval: 64
  save_eval_data: true

# Experiment-specific paths
paths:
  pair_margin: "25"
  save_dir: multiround/runs/09_no_ref_dpo_m25
  # Override default pairs to use margin25
  pairs:
    train: data/pairs_margin25/by_das/clean/train.clean.jsonl
    val: data/pairs_margin25/by_das/clean/val.clean.jsonl
    test: data/pairs_margin25/by_das/clean/test.clean.jsonl

dpo:
  beta: 0.13
  sft_lambda: 0.10
  label_smoothing: 0.0

# Override training epochs to match epochs_per_round
training:
  epochs: 20  # Matched to epochs_per_round

# Override optimizer learning rate  
optimizer:
  lr: 0.00018  # 1.8e-4 optimized for DPO